const sudokuReducer = (state, action) => {
    switch (action.type) {
        case "something":
            
            break;
    
        default:
            break;
    }
  return
}
export default sudokuReducer
